<%= form_for(@request) do |f| %>
  <%= f.text_field :email, :placeholder => "Email Address" %>
  <%= f.submit :class => "button" %>
  <%= link_to "Cancel", requests_path, :class => "button" %>
<% end %>
